# 패턴 가이드

이 문서는 현재 `metricstudio.patterns` 패키지에 정의된 패턴들의 사용법을 구현 기준으로 정리한 API 가이드다.

범위:
- `metricstudio/patterns/*.py`에 정의된 패턴
- `BasePattern` 공통 API
- 패턴 조합(`+`, `|`), 레짐 결합(`when()`), 거래 정책(`trade()`), 코호트 제한(`nmax()`)

기본 전제:
- 대부분의 패턴은 `Backtest.analyze(...)` 또는 `Backtest.run(...)` 안에서 사용하는 것을 전제로 한다.
- `high`, `low`, `volume`, `amount`, `marketcap` 같은 보조 필드는 `Backtest`가 자동으로 주입한다.
- `pattern(values)`를 직접 호출할 수도 있지만, 보조 필드가 필요한 패턴은 필요한 데이터가 미리 주입되어 있지 않으면 동작하지 않는다.

관련 문서:
- [API](API.md)
- [stay_cooldown_mask 매뉴얼](stay와%20cooldown.md)

생성자 규칙:
- 모든 패턴은 첫 positional 인자로 `name`을 받을 수 있다.
- 예: `p.Bollinger("볼린저돌파")`, `p.MFI("MFI상승")`
- window 같은 구조 파라미터가 있는 패턴은 `p.Bollinger("bb", window=20)` 형태를 권장한다.

## 1. 공통 사용법

기본 형태:

```python
from metricstudio import patterns as p

pat = p.Bollinger("볼린저돌파").on(
    trigger="breakout_up",
    breakout_cooldown_days=3,
    bandwidth_max=0.05,
)
```

백테스트에 연결:

```python
from metricstudio import Backtest, Univ
from metricstudio import patterns as p

bt = Backtest(
    start="2000-01-01",
    end="2025-12-31",
    by="day",
    univ=Univ(market=["KOSPI", "KOSDAQ"]),
)

pat = (
    p.Bollinger("bb").on(trigger="breakout_up", bandwidth_max=0.05)
    + p.High(name="52w").on(window=240, threshold=0.90)
    + p.Trending(name="ma200").on(trigger="ma_trend_up", window=200)
    + p.MFI("mfi50").on(trigger="above", threshold=50)
    + p.AmountSurge(name="amt15").on(window=20, threshold=1.5)
).named("trend_amount1.5x")

stats = bt.analyze(pat)
sim = bt.run(pattern="trend_amount1.5x", target_horizon="1M")
```

직접 평가:

```python
mask = pat(prices_np)
```

직접 평가의 출력:
- 타입: `np.ndarray`
- dtype: `bool`
- shape: 입력 시계열과 동일한 1차원 길이
- 의미: 해당 날짜에 진입 신호가 발생하면 `True`

주의:
- `MFI`, `AmountSurge`, `RetestBreakout`, `PanicRebound`, `SizeBucket` 등은 보조 필드가 필요하다.
- 이런 패턴은 `Backtest` 밖에서 직접 평가할 때 별도 데이터 준비가 필요하다.

## 2. 공통 API

### 2.1 `named(value)`

패턴 이름을 바꾼다.

```python
pat = p.Bollinger().on(...).named("trend_entry")
```

특징:
- 반환값은 자기 자신이다.
- `Backtest.analyze(...)`, `run(pattern=...)`, `plot(...)`에서 이 이름이 집계 키로 쓰인다.

### 2.1.1 `stay_days`, `cooldown_days` 공통 의미

여러 패턴의 `on(...)`에는 반복적으로 아래 두 파라미터가 나온다.

- `stay_days`: 조건이 연속으로 유지되어야 신호로 인정되는 최소 일수
- `cooldown_days`: 신호 발생 후 다음 신호까지 강제로 띄우는 최소 간격

구현상 대부분의 패턴은 내부적으로 `stay_cooldown_mask(condition, stay_days, cooldown_days)`를 사용한다.
이 두 파라미터의 정확한 동작 예시는 [stay_cooldown_mask 매뉴얼](stay와%20cooldown.md)을 참고하면 된다.

### 2.2 `when(regime)`

패턴을 특정 레짐 구간에서만 활성화한다.

```python
pat = p.MFI(name="contra").on(trigger="oversold_rebound").when(regime)
```

특징:
- 반환값은 새 wrapper 패턴이다.
- 원래 패턴의 진입 조건과 `regime.mask(...)`를 AND로 결합한다.
- 기본 레짐이 있는 `Backtest`에서는, 패턴에 별도 레짐이 없으면 기본 레짐이 자동 부착될 수 있다.

### 2.3 `market(market, field="close")`

종목 시계열 대신 시장 시계열을 기준으로 패턴을 계산하게 만든다.

```python
pat = p.Trending(name="kospi_ma60_down").market("kospi").on(
    window=60,
    trigger="ma_trend_down",
)
```

특징:
- 평가 기준이 각 종목 가격이 아니라 지정 시장의 시계열이 된다.
- 결과 mask는 날짜축 기준으로 동일하게 나오므로, 사실상 "시장 게이트"처럼 동작한다.
- `field`는 `open`, `high`, `low`, `close`, `volume`, `amount`, `marketcap` 중 하나를 쓸 수 있다.

### 2.4 `trim(quantile, method="remove")`

패턴 신호 자체가 아니라 `analyze()` 집계 시 극단값 처리를 설정한다.

```python
pat = p.Bollinger().on(...).trim(0.05, method="winsorize")
```

특징:
- `quantile=None`이면 trim 해제
- `method`는 `"remove"` 또는 `"winsorize"`
- 허용 범위는 `0.0 <= quantile < 0.5`

주의:
- `a + b` 또는 `a | b`로 결합할 때 trim 설정이 다르면 에러가 날 수 있다.

### 2.5 `trade(...)`

패턴 branch에 귀속될 거래 정책을 설정한다.

```python
pat = p.MFI().on(trigger="below", threshold=35).trade(
    target_horizon="3W",
    stop_loss_pct=8,
    take_profit_pct=None,
    cohort_scale=0.5,
)
```

입력:
- `target_horizon`: `"1W"`, `"1M"` 같은 horizon 라벨 또는 양의 정수
- `stop_loss_pct`: 손절 퍼센트. `0~1` 소수 또는 `1~100` 퍼센트 입력 가능
- `take_profit_pct`: 익절 퍼센트. 형식은 동일
- `cohort_scale`: branch 노출 비중. `0 < x <= 1`

특징:
- 신호 생성 규칙은 바꾸지 않는다.
- `run()` 시 branch별 보유기간/손절/익절/비중 정책으로 사용된다.

주의:
- `a + b`에서는 양쪽 `trade` 설정이 다르면 에러가 난다.
- `a | b`에서는 branch별로 서로 다른 `trade` 정책을 둘 수 있다.
- `a | b`에 직접 `trade(...)`를 주면 union 전체에 공통 정책을 덮어쓴다.

### 2.6 `nmax(value, market_cap=False)`

하루 신규 코호트 종목 수 상한을 패턴에 붙인다.

```python
pat = pat.nmax(10)
pat = pat.nmax(5, market_cap=True)
```

입력:
- `value`: 하루 최대 신규 종목 수
- `market_cap=True`: 시가총액을 1순위로 사용

특징:
- 신호가 너무 많이 발생한 날 상위 종목만 남긴다.
- 기본 정렬 우선순위는 패턴 구성에 따라 내부 metric을 사용한다.
- `market_cap=True`이면 시가총액이 우선된다.

주의:
- `a + b` 또는 `a | b`에서 서로 다른 `nmax` 설정은 충돌할 수 있다.
- 최종 패턴에 `nmax(...)`를 한 번만 주는 방식이 가장 명확하다.

### 2.7 패턴 결합: `+`

`a + b`는 교집합이다.

```python
pat = bb + high52w + uptrend
```

동작:
- 같은 날짜에 두 패턴이 모두 `True`일 때만 `True`
- exit rule은 양쪽 exit의 OR로 합쳐진다
- 청산 시점이 둘 다 있으면 더 이른 시점을 사용한다

### 2.8 패턴 결합: `|`

`a | b`는 합집합이다.

```python
router = trend.when(~contrarian) | contra.when(contrarian)
```

동작:
- 같은 날짜에 둘 중 하나라도 `True`면 `True`
- branch별 `trade(...)` 정책을 유지한 router 패턴을 만들 수 있다

## 3. 패턴 요약표

| 패턴 | 목적 | 추가 필드 | 핵심 입력 |
| --- | --- | --- | --- |
| `AllStockPattern` | 유효 가격 전체 benchmark | 없음 | 없음 |
| `AmountSurge` | 거래대금 급증 | `amount` | `window`, `threshold` |
| `Bollinger` | 밴드 수축 + 돌파/근접 | `high`, `low`는 trailing stop일 때만 | `trigger`, `bandwidth_*` |
| `Disparity` | 이동평균 대비 이격도 하방 | 없음 | `window`, `threshold` |
| `GoldenCross` | 다중 이동평균 정배열 상태 | 없음 | `windows` |
| `High` | 최근 고가권 근접/돌파 | 없음 | `window`, `threshold` |
| `MFI` | 과매도/반등/상단/하단 MFI | `high`, `low`, `volume` | `trigger`, `lower`, `threshold` |
| `PanicRebound` | 급락 후 반등 초입 | `volume`은 선택 | `drawdown_*`, `rebound_days` |
| `RelativeStrength` | 시장 대비 상대수익률 | 시장 `close` | `market`, `window`, `trigger`, `threshold` |
| `RetestBreakout` | 돌파 후 눌림목 재돌파 | `amount`는 선택 | `breakout_window`, `retest_tolerance` |
| `SizeBucket` | 시가총액 large/mid/small | 내부 `marketcap` 기반 bucket | `bucket` |
| `Trending` | 이동평균 돌파 또는 기울기 추세 | 없음 | `window`, `trigger` |

## 4. 패턴별 상세 문서

### 4.1 `AllStockPattern`

목적:
- 모든 유효 가격 구간을 선택하는 benchmark 패턴

생성:

```python
pat = p.AllStockPattern(name="benchmark")
```

입력:
- 별도 `on(...)` 없음

출력:
- `close`가 finite 이고 `> 0`인 날짜에서 `True`
- 사실상 "해당 유니버스 전체 종목" 기준 mask

주 용도:
- benchmark
- 레짐 내부 benchmark

예시:

```python
benchmark = p.AllStockPattern(name="benchmark").when(regime)
```

### 4.2 `AmountSurge`

목적:
- 거래대금이 최근 평균 대비 급증한 날짜를 찾는다.

생성:

```python
pat = p.AmountSurge(name="amt15").on(
    window=20,
    threshold=1.5,
    stay_days=1,
    cooldown_days=0,
)
```

입력:
- `window`: 거래대금 평균 window
- `threshold`: `amount / rolling_mean(amount, window)`의 최소 배수
- `stay_days`: 조건이 연속으로 유지되어야 하는 최소 일수
- `cooldown_days`: 신호 발생 후 재발생 차단 일수

필요 데이터:
- `amount`

출력:
- 해당 날짜의 거래대금이 최근 `window`일 평균의 `threshold`배 이상이면 `True`
- 이후 `stay_days`, `cooldown_days`가 적용된 bool mask 반환

해석:
- `threshold=1.5`는 최근 평균 거래대금의 1.5배 이상

예시:

```python
amt = p.AmountSurge(name="거래대금1.5x").on(window=20, threshold=1.5)
```

### 4.3 `Bollinger`

목적:
- 볼린저 밴드 수축 구간 위에서 상단/하단 돌파 또는 밴드 근접을 감지한다.

생성:

```python
pat = p.Bollinger("bb", window=20, sigma=2.0).on(
    trigger="breakout_up",
    bandwidth_min=0.0,
    bandwidth_max=0.05,
    bandwidth_stay_days=1,
    bandwidth_type="absolute",
    bandwidth_percentile_window=240,
    breakout_cooldown_days=3,
    near_tolerance=0.03,
    near_stay_days=1,
)
```

생성자 입력:
- `window`: 이동평균/표준편차 window
- `sigma`: 밴드 폭 계수

`on(...)` 입력:
- `trigger`: `"breakout_up"`, `"breakout_down"`, `"near_up"`, `"near_down"`, 또는 `None`
- `bandwidth_min`: 밴드 폭 하한
- `bandwidth_max`: 밴드 폭 상한
- `bandwidth_stay_days`: 밴드 폭 조건 유지 일수
- `bandwidth_type`: `"absolute"` 또는 `"percentile"`
- `bandwidth_percentile_window`: percentile 모드 lookback
- `breakout_cooldown_days`: 돌파 신호 쿨다운
- `near_tolerance`: 밴드 근접 허용 오차
- `near_stay_days`: 근접 조건 유지 일수

필요 데이터:
- 추가 필드 없음

출력:
- 먼저 밴드 폭 조건을 만족하는 날짜를 고른다.
- 그 위에서 `trigger`에 따라 신호를 만든다.

밴드 폭 정의:
- 내부 ratio는 `band_width / mean`
- `band_width = sigma * rolling_std`

`bandwidth_type="absolute"`:
- `bandwidth_min <= band_width / mean <= bandwidth_max`

`bandwidth_type="percentile"`:
- 현재 `band_width / mean`이 과거 `bandwidth_percentile_window`일 분포에서
  지정 percentile 구간 안에 들어오면 `True`
- 예를 들어 `bandwidth_min=0.0`, `bandwidth_max=0.2`는 하위 20% 폭 구간 의미

`trigger` 의미:
- `"breakout_up"`: `price > upper_band`
- `"breakout_down"`: `price < lower_band`
- `"near_up"`: `price >= upper_band * (1 - near_tolerance)`
- `"near_down"`: `price <= lower_band * (1 + near_tolerance)`
- `None`: 순수 밴드 폭 조건만 사용

예시:

```python
bb = p.Bollinger("볼린저돌파").on(
    trigger="breakout_up",
    breakout_cooldown_days=3,
    bandwidth_max=0.05,
)

bb_near = p.Bollinger("볼린저하단근접").on(
    trigger="near_down",
    near_tolerance=0.02,
)
```

### 4.4 `Disparity`

목적:
- 가격이 이동평균 대비 충분히 아래에 있는 구간을 감지한다.

생성:

```python
pat = p.Disparity("disp20", window=20).on(
    threshold=0.90,
    stay_days=1,
    cooldown_days=0,
)
```

생성자 입력:
- `window`: 이동평균 window

`on(...)` 입력:
- `threshold`: disparity 상한
- `stay_days`: 연속 유지 일수
- `cooldown_days`: 쿨다운 일수

출력:
- `disparity = close / moving_average(window)`
- `disparity < threshold`이면 `True`

해석:
- `threshold=0.90`이면 가격이 이동평균의 90% 미만인 날짜

예시:

```python
disp = p.Disparity("disparity0.9", window=20).on(
    threshold=0.9,
    stay_days=5,
    cooldown_days=5,
)
```

### 4.5 `GoldenCross`

목적:
- 여러 이동평균선이 정배열 상태인지 감지한다.

생성:

```python
pat = p.GoldenCross(name="gc").on(
    windows=(5, 10, 20),
    stay_days=1,
    cooldown_days=3,
)
```

입력:
- `windows`: 오름차순 이동평균 window 목록
- `stay_days`: 정배열 상태 유지 일수
- `cooldown_days`: 쿨다운 일수

출력:
- 각 날짜에서 `MA(5) > MA(10) > MA(20)` 같은 정배열이면 `True`

중요:
- "실제 교차 순간"을 잡는 패턴이 아니라 "정배열 상태"를 잡는 패턴이다.

예시:

```python
gc = p.GoldenCross(name="gc_5_10_20").on(windows=(5, 10, 20))
```

### 4.6 `High`

목적:
- 최근 고가권 근처에 있는 날짜를 감지한다.

생성:

```python
pat = p.High(name="52w_high").on(
    window=240,
    threshold=0.90,
    stay_days=1,
    cooldown_days=0,
)
```

입력:
- `window`: rolling high window
- `threshold`: 최근 고가 대비 비율
- `stay_days`: 유지 일수
- `cooldown_days`: 쿨다운 일수

출력:
- `price >= threshold * rolling_high(window)`이면 `True`

해석:
- `window=240`은 대략 1년
- `threshold=0.90`이면 최근 240일 최고가의 90% 이상
- `threshold=1.0`이면 rolling high 이상

예시:

```python
high52w = p.High(name="52주고가").on(window=240, threshold=0.90, stay_days=1)
```

### 4.7 `MFI`

목적:
- Money Flow Index 기반 과매도/반등/상단/하단 조건을 잡는다.

생성:

```python
pat = p.MFI("mfi", window=14).on(
    trigger="oversold_rebound",
    lower=20,
    threshold=None,
    stay_days=1,
    cooldown_days=0,
)
```

생성자 입력:
- `window`: MFI window

`on(...)` 입력:
- `trigger`: `"oversold_rebound"`, `"bullish_failure_swing"`, `"above"`, `"below"`
- `lower`: oversold 기준선
- `threshold`: `above`/`below`에서 사용할 절대 기준선
- `stay_days`: 유지 일수
- `cooldown_days`: 쿨다운 일수

필요 데이터:
- `high`, `low`, `volume`

출력:
- `"above"`: `MFI > threshold`
- `"below"`: `MFI < threshold`
- `"oversold_rebound"`: 전일 `MFI <= lower` 이고 당일 `MFI > lower`
- `"bullish_failure_swing"`: oversold 이후 반등, 눌림, 재상승 구조를 상태기계로 감지

해석:
- `threshold`, `lower`의 단위는 모두 `0~100` MFI 값

예시:

```python
mfi_high = p.MFI("MFI상승").on(trigger="above", threshold=50)
mfi_low = p.MFI("MFI<35").on(trigger="below", threshold=35)
mfi_rebound = p.MFI("mfi_oversold").on(
    trigger="oversold_rebound",
    lower=20,
    cooldown_days=5,
)
```

### 4.8 `PanicRebound`

목적:
- 최근 급락 이후 짧은 연속 반등이 시작되는 구간을 잡는다.

생성:

```python
pat = p.PanicRebound(name="panic_rebound").on(
    drawdown_window=20,
    drawdown_min=-0.18,
    rebound_days=3,
    volume_spike=True,
    volume_window=20,
    volume_threshold=1.5,
    stay_days=1,
    cooldown_days=0,
)
```

입력:
- `drawdown_window`: 최근 고점 기준 drawdown 계산 window
- `drawdown_min`: 최소 drawdown 조건. 음수여야 함
- `rebound_days`: 연속 상승 마감 일수
- `volume_spike`: 거래량 스파이크 조건 사용 여부
- `volume_window`: 거래량 평균 window
- `volume_threshold`: 거래량 배수
- `stay_days`: 유지 일수
- `cooldown_days`: 쿨다운 일수

필요 데이터:
- `volume_spike=True`일 때 `volume`

출력:
- 최근 `drawdown_window`일 rolling high 대비 낙폭이 `drawdown_min` 이하인 panic 구간이 있어야 한다.
- 그 뒤 `rebound_days`일 연속 종가 상승이 확인되면 `True`
- `volume_spike=True`이면 마지막 반등일에 거래량 조건도 만족해야 한다.

해석:
- `drawdown_min=-0.18`이면 최근 고점 대비 18% 이상 하락
- `rebound_days=3`이면 3일 연속 종가 상승 필요

예시:

```python
panic = p.PanicRebound(name="panic_rebound").on(
    drawdown_window=20,
    drawdown_min=-0.18,
    rebound_days=3,
    volume_spike=False,
)
```

### 4.9 `RelativeStrength`

목적:
- 시장 대비 일정 기간 상대수익률 우위/열위를 감지한다.

생성:

```python
pat = p.RelativeStrength(name="rs").on(
    market="kospi",
    window=60,
    trigger="above",
    threshold=0.0,
    stay_days=1,
    cooldown_days=0,
)
```

입력:
- `market`: 비교 기준 시장
- `window`: 수익률 비교 기간
- `trigger`: `"above"` 또는 `"below"`
- `threshold`: 상대수익률 기준
- `stay_days`: 유지 일수
- `cooldown_days`: 쿨다운 일수

필요 데이터:
- 시장 `close`

출력:
- `stock_ret = close[t] / close[t-window] - 1`
- `market_ret = market_close[t] / market_close[t-window] - 1`
- `rel = stock_ret - market_ret`
- `"above"`면 `rel >= threshold`
- `"below"`면 `rel <= threshold`

해석:
- `threshold=0.05`는 시장 대비 `+5%p`
- `threshold=-0.08`는 시장 대비 `-8%p` 이하

사용 방식 2가지:

```python
rs = p.RelativeStrength(name="rs").on(market="kospi", window=60, threshold=0.0)
rs = p.RelativeStrength(name="rs").market("kospi").on(window=60, threshold=0.0)
```

예시:

```python
loser5 = p.RelativeStrength(name="5D상대낙폭").on(
    market="kospi",
    window=5,
    trigger="below",
    threshold=-0.08,
    cooldown_days=5,
)
```

### 4.10 `RetestBreakout`

목적:
- 직전 고점 돌파 후, 눌림을 거쳐 다시 돌파 레벨을 회복하는 패턴을 찾는다.

생성:

```python
pat = p.RetestBreakout(name="retest").on(
    breakout_window=20,
    retest_tolerance=0.03,
    max_retest_days=10,
    breakout_amount_threshold=None,
    breakout_amount_window=20,
    rebound_confirm="close_up",
    stay_days=1,
    cooldown_days=0,
)
```

입력:
- `breakout_window`: prior high 계산 window
- `retest_tolerance`: 눌림 허용폭
- `max_retest_days`: 돌파 후 설정 유지 최대 일수
- `breakout_amount_threshold`: 돌파일 거래대금 급증 배수
- `breakout_amount_window`: 거래대금 평균 window
- `rebound_confirm`: 현재는 `"close_up"`만 지원
- `stay_days`: 유지 일수
- `cooldown_days`: 쿨다운 일수

필요 데이터:
- `breakout_amount_threshold`를 쓰면 `amount`

출력:
- 먼저 가격이 직전 `breakout_window`일 rolling high를 돌파해야 한다.
- 이후 가격이 `active_level * (1 - retest_tolerance)` 이상 구간에서 눌림을 만든다.
- 그 다음 다시 상승하며 `active_level` 이상을 회복하면 신호가 발생한다.
- `max_retest_days`를 넘기거나 하단 허용폭을 깨면 setup은 폐기된다.

해석:
- `retest_tolerance=0.03`이면 돌파 레벨 아래 3%까지 눌림 허용
- `breakout_amount_threshold=1.5`면 돌파일 거래대금이 평균의 1.5배 이상이어야 setup 인정

예시:

```python
retest = p.RetestBreakout(name="retest").on(
    breakout_window=20,
    retest_tolerance=0.03,
    max_retest_days=10,
    breakout_amount_threshold=1.5,
    breakout_amount_window=20,
)
```

### 4.11 `SizeBucket`

목적:
- 시가총액 bucket 자체를 패턴처럼 사용할 수 있게 노출한다.

생성:

```python
pat = p.SizeBucket(name="large_cap").on(bucket="large")
```

입력:
- `bucket`: `"large"`, `"mid"`, `"small"`

필요 데이터:
- 내부적으로 `marketcap`에서 파생된 `size_bucket_large`, `size_bucket_mid`, `size_bucket_small`

출력:
- 해당 날짜, 해당 종목이 지정 bucket에 속하면 `True`

bucket 정의:
- 일별 시가총액 내림차순 정렬
- 누적 시가총액 비중 기준
- `large`: 상위 50%
- `mid`: 다음 25%
- `small`: 나머지 25%

예시:

```python
small = p.SizeBucket(name="small").on(bucket="small")
```

### 4.12 `Trending`

목적:
- 이동평균 돌파 또는 이동평균 기울기 방향을 추세 신호로 사용한다.

생성:

```python
pat = p.Trending(name="trend").on(
    window=20,
    trigger="breakout_up",
    stay_days=1,
    cooldown_days=0,
)
```

입력:
- `window`: 이동평균 window
- `trigger`: `"breakout_up"`, `"breakout_down"`, `"ma_trend_up"`, `"ma_trend_down"`
- `stay_days`: 유지 일수
- `cooldown_days`: 쿨다운 일수

출력:
- `"breakout_up"`: `price > MA(window)`
- `"breakout_down"`: `price < MA(window)`
- `"ma_trend_up"`: `MA(window)[t] > MA(window)[t-1]`
- `"ma_trend_down"`: `MA(window)[t] < MA(window)[t-1]`

중요:
- `ma_trend_up/down`은 가격 돌파가 아니라 이동평균의 기울기 방향이다.
- 시장 게이트로 자주 쓰려면 `.market("kospi")`와 함께 사용하면 된다.

예시:

```python
uptrend = p.Trending(name="ma200_up").on(trigger="ma_trend_up", window=200)
ma60_down = p.Trending(name="kospi_ma60_down").market("kospi").on(
    trigger="ma_trend_down",
    window=60,
)
```

## 5. 실전 조합 예시

### 5.1 추세형 기본 패턴

```python
trend = (
    p.Bollinger(name="bb").on(trigger="breakout_up", breakout_cooldown_days=3, bandwidth_max=0.05)
    + p.High(name="52w").on(window=240, threshold=0.90, stay_days=1)
    + p.Trending(name="ma200").on(trigger="ma_trend_up", window=200)
    + p.MFI(name="mfi50").on(trigger="above", threshold=50)
    + p.AmountSurge(name="amt15").on(window=20, threshold=1.5)
).named("trend_amount1.5x")
```

### 5.2 contrarian 패턴

```python
contra = (
    p.RelativeStrength(name="loser5").on(
        market="kospi",
        window=5,
        trigger="below",
        threshold=-0.08,
        cooldown_days=5,
    )
    + p.MFI(name="mfi35").on(trigger="below", threshold=35, stay_days=1, cooldown_days=0)
).named("loser5_mfi35")
```

### 5.3 레짐 router

```python
router = (
    trend.when(~contrarian_regime)
    | contra.trade(target_horizon="3W", stop_loss_pct=8).when(contrarian_regime)
).named("switch_on_contrarian")
```

### 5.4 코호트 제한

```python
trend_top10 = trend.nmax(10)
trend_top5_mcap = trend.nmax(5, market_cap=True)
```

## 6. 자주 헷갈리는 포인트

`GoldenCross`:
- 이름은 골든크로스지만 "교차 순간"이 아니라 "정배열 상태"를 잡는다.

`High`:
- 최근 고점 "돌파"만이 아니라, `threshold < 1.0`이면 고점 근처 구간도 잡는다.

`Trending(ma_trend_up/down)`:
- 가격과 이동평균의 위치 관계가 아니라 이동평균의 방향이다.

`RelativeStrength.threshold`:
- 퍼센트가 아니라 절대 수익률 차이 값이다.
- 예를 들어 `-0.08`은 `-8%p`를 뜻한다.

`Bollinger.bandwidth_type="percentile"`:
- `bandwidth_min`, `bandwidth_max`는 0~1 비율 percentile 구간이다.
- `0.0 ~ 0.2`는 하위 20% 폭 구간이다.

`trade(...)`:
- 신호 생성 규칙이 아니라 `run()` 단계 보유 정책이다.

`trim(...)`:
- 패턴 신호가 아니라 `analyze()` 수익률 집계 방식에 영향을 준다.

`SizeBucket`:
- 고정 종목군이 아니라 일별 시가총액 비중 기준 bucket이다.

## 7. 추천 사용 순서

1. 단일 패턴을 `.on(...)`으로 정의한다.
2. 필요하면 `.market(...)` 또는 `.when(...)`을 붙인다.
3. 패턴 여러 개를 `+` 또는 `|`로 조합한다.
4. 최종 패턴에 `.named(...)`를 붙인다.
5. 운용 제어가 필요하면 마지막에 `.trade(...)`, `.nmax(...)`, `.trim(...)`을 붙인다.

예:

```python
pat = (
    p.Bollinger().on(trigger="breakout_up", bandwidth_max=0.05)
    + p.High().on(window=240, threshold=0.90)
    + p.Trending().on(trigger="ma_trend_up", window=200)
).named("trend_core").nmax(10)
```
