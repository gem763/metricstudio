# 레짐 설계안

## 현재 상태 요약

- 이 문서는 과거 실험 메모가 아니라 현재 코드 기준 설명으로 다시 정리한 버전이다.
- 현재 레짐 구현 파일은 `metricstudio/regime.py`이고, 백테스트 연결은 `metricstudio/backtest.py`에 있다.
- 현재 패턴 구현은 더 이상 `src/pattern.py` 기준이 아니라 `metricstudio/patterns/` 아래 모듈들 기준이다.
- 현재 레짐 체계는 "상위 4개 regime + 보조 2개 tag + legacy alias" 구조다.
- 중요: 현재 기준 레짐은 백테스트에서 사용하지 않는다. `nb/실험 2026.03.13.ipynb` 기준 기본 백테스트는 `Backtest(start, end, benchmark=bm, by='day', univ=univ)`만 만들고, `regime=` 인자, `bt.regime` 할당, `Pattern.when(Regime(...))` 호출을 하지 않는다.

즉 지금 레짐 코드는 "구현되어 있는 기능"이고, 현 시점 노트북 기준 백테스트는 "레짐을 붙이지 않은 baseline"이다. 최근 실험 결과를 읽을 때도 이 점을 전제로 해야 한다.

---

## 1) 배경

과거에는 아래와 같은 5레짐 설명이 중심이었다.

- `quiet_squeeze_expansion`
- `broad_bull_breakout`
- `narrow_leadership`
- `panic_rebound_risk`
- `sideways_choppy`

문제는 이 분류가 시장 설명용 taxonomy로는 직관적이지만, 실제 패턴 스위치 관점에서는 해상도가 과하다는 점이었다.

특히 trend 계열 패턴은 종목 단에서 이미 아래 정보를 상당 부분 포함한다.

- squeeze / breakout
- 절대 강세
- 장기 추세 정렬
- 거래 강도 확인

그래서 상위 레짐도 같은 축을 너무 잘게 다시 자르면 표본만 줄고, 실제 의사결정 차이는 작아질 수 있었다. 그 결과 현재 구현은 상위 레짐을 단순화하고, 일부 설명용 상태는 tag나 legacy label로 내리는 방향으로 정리되었다.

---

## 2) 현재 구현 구조

현재 `metricstudio/regime.py`는 아래 이름들을 직접 계산한다.

### 상위 4개 regime

- `trend_friendly`
- `contrarian_friendly`
- `panic_rebound_risk`
- `neutral`

### 보조 tag

- `quiet_tag`
- `narrow_tag`

### legacy 호환 이름

- `quiet_squeeze_expansion`
- `broad_bull_breakout`
- `narrow_leadership`
- `sideways_choppy`

legacy 이름은 과거 실험 결과나 예전 코드와의 호환을 위해 남아 있지만, 현재 top-level 분류의 중심은 아니다.

---

## 3) `build_regime_frame()`가 만드는 결과

`build_regime_frame()`은 시장 지수, 개별 종목 close, amount, market cap을 이용해 일자별 레짐 테이블을 만든다.

반환값에는 아래가 함께 들어간다.

- 지표 계산용 중간 컬럼
- 현재 체계용 boolean 컬럼
- legacy 비교용 boolean 컬럼
- `label`: 현재 상위 4개 regime 기준 대표 라벨
- `legacy_label`: 과거 5레짐 기준 대표 라벨
- `unavailable`: 계산에 필요한 최소 지표가 아직 안 갖춰진 구간

즉 현재 구조는 "하나의 label만 갖는 체계"가 아니라, 여러 boolean mask와 요약 label을 같이 보유하는 형태다.

---

## 4) 현재 레짐 정의

아래 내용은 현재 `metricstudio/regime.py`의 실제 구현을 그대로 요약한 것이다.

### 공통 전제

모든 top-level regime과 tag는 필요한 입력 지표가 모두 계산된 날짜에서만 유효하다. 데이터가 부족한 초반 구간은 `unavailable=True`로 남는다.

### `panic_rebound_risk`

시장 스트레스가 큰 구간을 분리한다.

```python
panic = (
    (rv20_pct240 >= 0.85)
    or ((dd60 <= -0.10) and (pct_above20_all < 0.35))
)
```

### `quiet_tag`

저변동 압축 성격만 따로 표시하는 보조 tag다.

```python
quiet_tag = (
    (bbw20_pct240 <= 0.20)
    and (rv20_pct240 <= 0.40)
)
```

### `narrow_tag`

대형주 주도 편중 상태를 표시하는 보조 tag다.

```python
narrow_tag = (
    (trend_score >= 2.0)
    and (large_pct_above60 >= 0.60)
    and (small_pct_above60 <= 0.45)
    and (leadership_spread >= 0.15)
)
```

### `trend_friendly`

현재 구현의 핵심은 `quiet`, `broad`, `narrow`를 각각 독립 top-level regime으로 유지하지 않고, 추세 친화 환경 안의 분기들로 흡수한 점이다.

```python
trend_common_gate = (
    (not panic)
    and (trend_score >= 2.0)
    and (dd60 > -0.08)
    and (rv20_pct240 < 0.80)
)

trend_quiet_branch = (
    trend_common_gate
    and (bbw20_pct240 <= 0.20)
    and (rv20_pct240 <= 0.40)
    and (pct_above20_delta5 >= 0.05)
    and (pct_above20_all >= 0.45)
)

trend_broad_branch = (
    trend_common_gate
    and (pct_above60_all >= 0.50)
    and (aar5 >= 0.50)
)

trend_narrow_branch = (
    trend_common_gate
    and (large_pct_above60 >= 0.60)
    and (small_pct_above60 <= 0.45)
    and (leadership_spread >= 0.15)
    and (aar5 >= 0.45)
)

trend_friendly = (
    trend_quiet_branch
    or trend_broad_branch
    or trend_narrow_branch
)
```

해석은 아래처럼 잡으면 된다.

- `trend_quiet_branch`: 압축형 추세 확장
- `trend_broad_branch`: breadth가 확인된 확산형 추세
- `trend_narrow_branch`: 대형주 주도형 추세를 제한적으로 허용한 예외 분기

즉 현재 구현은 예전 `QUIET`, `BROAD`, `NARROW`를 그대로 병렬 운용하는 구조가 아니다.

### `contrarian_friendly`

panic은 아니고 trend도 아닌데, 반등/이격 해소 패턴이 더 자연스러운 구간을 분리한다.

```python
contrarian_friendly = (
    (not panic)
    and (not trend_friendly)
    and (
        (
            (dd60 <= -0.05)
            and (pct_above20_all <= 0.45)
            and (pct_above20_delta5 >= -0.03)
        )
        or
        (
            (rv20_pct240 >= 0.55)
            and (pct_above20_all >= 0.30)
            and (pct_above20_all <= 0.50)
            and (aar5 >= 0.45)
        )
    )
)
```

### `neutral`

명시적 추세 우호도 아니고, 역추세 우호도 아니고, panic도 아닌 나머지다.

```python
neutral = (
    (not panic)
    and (not trend_friendly)
    and (not contrarian_friendly)
)
```

---

## 5) legacy label의 현재 의미

과거 이름들은 현재도 bool 컬럼과 `legacy_label`로 남아 있다. 다만 이들은 현재 운용 체계의 중심이라기보다 아래 용도에 가깝다.

- 과거 실험 결과와의 비교
- 예전 notebook / 문서 / 메모와의 연결
- 기존 `kind` 입력값 호환

예를 들어 아래 입력은 여전히 허용된다.

```python
Regime().on(kind="broad_bull_breakout", market="kospi")
Regime().on(kind="quiet_squeeze_expansion", market="kospi")
```

하지만 현재 구조를 설명할 때는 이 legacy 이름보다 `trend_friendly`, `contrarian_friendly`, `panic_rebound_risk`, `neutral`을 기준으로 이해하는 편이 맞다.

---

## 6) Backtest 연결 방식

현재 `metricstudio/backtest.py`는 레짐을 붙일 수 있는 API를 이미 제공한다.

### 방법 1: `Backtest(..., regime=...)`

```python
bt = Backtest(
    start,
    end,
    benchmark=bm,
    regime=Regime().on(kind="trend_friendly", market="kospi"),
    by="day",
    univ=univ,
)
```

이 경우 `Backtest` 기본 레짐이 있고 패턴에 별도 레짐이 없으면 `pattern.when(self.regime)`로 자동 적용된다.

### 방법 2: 패턴에 직접 부착

```python
pattern = some_pattern.when(Regime().on(kind="panic_rebound_risk", market="kospi"))
```

### 방법 3: 합성 selector

현재 `Regime` 객체는 단일 leaf뿐 아니라 조합식도 지원한다.

- `~regime`: 반전
- `left + right`: and
- `left | right`: or
- `left - right`: and not

즉 구현상 레짐 기능은 살아 있다. 다만 아래가 더 중요하다.

---

## 7) 현재는 백테스트에서 사용하지 않는다

이 문서에서 가장 중요한 현재 상태는 이것이다.

- `nb/실험 2026.03.13.ipynb` 기준 백테스트는 레짐을 붙이지 않는다.
- 해당 노트북에는 `Regime(...)`, `bt.regime = ...`, `pattern.when(Regime(...))` 사용이 없다.
- 따라서 현재 노트북에서 보는 성과 표와 차트는 레짐 필터가 없는 baseline 결과다.

즉 현재 레짐은 "코드에 구현되어 있으나, 현행 실험 흐름에서는 꺼져 있는 기능"으로 보는 편이 정확하다.

이 점을 문서에 명시하는 이유는 간단하다.

- 레짐 코드가 존재한다고 해서 현재 실험이 자동으로 레짐 기반인 것은 아니다.
- 최근 notebook 결과를 해석할 때 "이 패턴이 trend regime에서 검증된 결과"라고 읽으면 오해가 된다.
- 실제로는 레짐을 다시 실전에 연결할지 여부 자체가 별도 판단 사항이다.

---

## 8) 현재 문서를 읽는 방법

현재 이 문서는 아래 용도로만 읽는 것이 맞다.

- `metricstudio/regime.py`가 지금 어떤 체계를 계산하는지 이해
- legacy 이름과 현재 이름의 대응 관계 확인
- 나중에 레짐을 다시 백테스트에 연결할 때 기준점으로 사용

반대로 아래처럼 읽으면 안 된다.

- 현재 notebook 백테스트가 이미 레짐 라우팅을 사용한다고 가정
- 과거 5레짐 설명을 현재 실전 운용 상태로 오해
- `quiet`, `broad`, `narrow`가 지금도 top-level regime으로 그대로 쓰인다고 해석

---

## 9) 실무적 정리

현재 기준으로 정리하면 다음 한 문장으로 요약할 수 있다.

현재 코드는 "4개 상위 regime + 2개 tag + legacy 호환" 구조를 구현하고 있지만, `실험 2026.03.13.ipynb` 기준 백테스트는 아직 그 레짐을 사용하지 않는다.
