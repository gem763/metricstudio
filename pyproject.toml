[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "metricstudio"
version = "0.1.0"
description = "Local backtesting and pattern research toolkit"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["metricstudio*"]
