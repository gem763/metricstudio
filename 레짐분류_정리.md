좋아. 아래는 **바로 코드로 옮길 수 있게** 애매한 표현을 없애고 고정한 버전이다.

기준은 다음으로 통일하자.

- **데이터 주기**: 일봉
- **레짐 판정 시점**: `t`일 **종가 기준**
- **전략 적용 시점**: `t+1`일 이후 진입/청산
- **시장**: KOSPI
- **유니버스 `U_t`**: `t`일에 가격/거래량/거래대금/시총/상장주수 값이 모두 유효한 종목
- **시총 버킷**:
  - 대형주: `시총순위_t <= 100`
  - 중형주: `101 <= 시총순위_t <= 300`
  - 소형주: `시총순위_t >= 301`

---

## 1) 기초 변수 정의표

| 변수명 | 정의 |
|---|---|
| `ret1_i,t` | `close_i,t / close_i,t-1 - 1` |
| `logret_kospi,t` | `ln(kospi_close_t / kospi_close_t-1)` |
| `sma_n(x)_t` | 최근 `n`일 단순이동평균 |
| `std_n(x)_t` | 최근 `n`일 표준편차 |
| `upper20_t` | `sma20(kospi_close)_t + 2 * std20(kospi_close)_t` |
| `lower20_t` | `sma20(kospi_close)_t - 2 * std20(kospi_close)_t` |
| `BBW20_t` | `(upper20_t - lower20_t) / sma20(kospi_close)_t` |
| `RV20_t` | `std_20(logret_kospi)_t` |
| `DD60_t` | `kospi_close_t / max(kospi_close_{t-59:t}) - 1` |
| `TrendScore_t` | `1[kospi_close_t > sma240(kospi_close)_t] + 1[sma240_t > sma240_{t-20}] + 1[kospi_close_t > sma60(kospi_close)_t]` |
| `PctAbove20_all_t` | `mean_i( 1[close_i,t > sma20(close_i)_t] )` over `U_t` |
| `PctAbove60_all_t` | `mean_i( 1[close_i,t > sma60(close_i)_t] )` over `U_t` |
| `PctAbove20Delta5_t` | `PctAbove20_all_t - PctAbove20_all_{t-5}` |
| `AdvAmtRatio_t` | `sum_i(거래대금_i,t * 1[ret1_i,t > 0]) / sum_i(거래대금_i,t)` |
| `AAR5_t` | 최근 5일 `AdvAmtRatio` 평균 |
| `LargePctAbove60_t` | 대형주 집합에서 `mean_i(1[close_i,t > sma60_i,t])` |
| `MidPctAbove60_t` | 중형주 집합에서 `mean_i(1[close_i,t > sma60_i,t])` |
| `SmallPctAbove60_t` | 소형주 집합에서 `mean_i(1[close_i,t > sma60_i,t])` |
| `LeadershipSpread_t` | `LargePctAbove60_t - SmallPctAbove60_t` |

### 퍼센타일 변수 정의
변동성/밴드폭은 절대값보다 **직전 252거래일 내 퍼센타일**로 쓰자.

| 변수명 | 정의 |
|---|---|
| `BBW20_pct252_t` | `BBW20_t`의 최근 252일 퍼센타일 랭크, 범위 `[0,1]` |
| `RV20_pct252_t` | `RV20_t`의 최근 252일 퍼센타일 랭크, 범위 `[0,1]` |

실제로는 판다스에서 `rolling(252)` 위에서 **마지막 값의 percentile rank**를 계산하면 된다.

---

## 2) 레짐 분류표

아래 표는 **서로 겹치지 않게** 우선순위까지 포함해서 정의한 것이다.  
반드시 **위에서 아래 순서대로** 판정한다.

| 우선순위 | 레짐 코드 | 분류 조건 |
|---|---|---|
| 1 | `PANIC_REBOUND_RISK` | `(RV20_pct252_t >= 0.85) OR ((DD60_t <= -0.10) AND (PctAbove20_all_t < 0.35))` |
| 2 | `QUIET_SQUEEZE_EXPANSION` | `(TrendScore_t >= 1) AND (BBW20_pct252_t <= 0.20) AND (RV20_pct252_t <= 0.40) AND (PctAbove20Delta5_t >= 0.05)` |
| 3 | `BROAD_BULL_BREAKOUT` | `(TrendScore_t >= 2) AND (PctAbove60_all_t >= 0.55) AND (AAR5_t >= 0.55) AND (RV20_pct252_t < 0.80) AND (DD60_t > -0.08)` |
| 4 | `NARROW_LEADERSHIP` | `(TrendScore_t >= 2) AND (LargePctAbove60_t >= 0.60) AND (SmallPctAbove60_t <= 0.45) AND (LeadershipSpread_t >= 0.15)` |
| 5 | `SIDEWAYS_CHOPPY` | 위 4개 중 어느 것도 만족하지 않음 |

---

## 3) 레짐 해석 및 전략 적용표

| 레짐 코드 | 해석 | 볼린저 상단돌파 전략 적용 |
|---|---|---|
| `PANIC_REBOUND_RISK` | 공포장, 급락 직후 고변동성 반등 위험 구간 | **신규 매수 금지** |
| `QUIET_SQUEEZE_EXPANSION` | 저변동성 압축 + 내부참여도 개선 | **스퀴즈형 돌파만 허용** |
| `BROAD_BULL_BREAKOUT` | 지수 추세 + 시장 참여 + 자금 유입이 동시에 양호 | **기본 레짐, 적극 허용** |
| `NARROW_LEADERSHIP` | 대형주 중심 상승, 시장 전체는 약함 | **대형주만 적극 허용**, 중소형주 제한 |
| `SIDEWAYS_CHOPPY` | 방향성 부족, 위아래 흔들림 많음 | **원칙적으로 비활성화** 또는 매우 보수적 적용 |

---

## 4) 종목 단위 진입 허용 규칙까지 명시한 표

이건 레짐이 분류된 뒤 **개별 종목 볼린저 돌파를 살릴지 말지**를 정하는 룰이다.

### 공통 종목 변수
- `stock_upper20_i,t = sma20(close_i)_t + 2*std20(close_i)_t`
- `stock_lower20_i,t = sma20(close_i)_t - 2*std20(close_i)_t`
- `stock_BBW20_i,t = (stock_upper20_i,t - stock_lower20_i,t) / sma20(close_i)_t`
- `stock_BBW20_pct252_i,t = 최근 252일 내 퍼센타일`
- `stock_trend_i,t = 1[close_i,t > sma60(close_i)_t] AND 1[sma60_i,t > sma60_i,t-20]`
- `stock_breakout_i,t = 1[close_i,t > stock_upper20_i,t]`

### 종목 진입 허용표

| 레짐 코드 | 종목 진입 허용 조건 |
|---|---|
| `PANIC_REBOUND_RISK` | 항상 `False` |
| `QUIET_SQUEEZE_EXPANSION` | `stock_breakout_i,t AND stock_trend_i,t AND (stock_BBW20_pct252_i,t <= 0.15)` |
| `BROAD_BULL_BREAKOUT` | `stock_breakout_i,t AND stock_trend_i,t` |
| `NARROW_LEADERSHIP` | 대형주면 `stock_breakout_i,t AND stock_trend_i,t`, 중형주는 여기에 `52주고가근접 >= 0.97` 추가, 소형주는 `False` |
| `SIDEWAYS_CHOPPY` | 원칙적으론 `False`; 굳이 돌릴 경우 `stock_breakout_i,t AND stock_trend_i,t AND (52주고가근접 >= 0.99)` |

---

## 5) 그대로 코드로 옮길 수 있는 최종 if/elif 구조

```python
if (RV20_pct252_t >= 0.85) or ((DD60_t <= -0.10) and (PctAbove20_all_t < 0.35)):
    regime_t = "PANIC_REBOUND_RISK"

elif (TrendScore_t >= 1) and (BBW20_pct252_t <= 0.20) and (RV20_pct252_t <= 0.40) and (PctAbove20Delta5_t >= 0.05):
    regime_t = "QUIET_SQUEEZE_EXPANSION"

elif (TrendScore_t >= 2) and (PctAbove60_all_t >= 0.55) and (AAR5_t >= 0.55) and (RV20_pct252_t < 0.80) and (DD60_t > -0.08):
    regime_t = "BROAD_BULL_BREAKOUT"

elif (TrendScore_t >= 2) and (LargePctAbove60_t >= 0.60) and (SmallPctAbove60_t <= 0.45) and (LeadershipSpread_t >= 0.15):
    regime_t = "NARROW_LEADERSHIP"

else:
    regime_t = "SIDEWAYS_CHOPPY"
```

---

## 6) 구현 시 주의사항

| 항목 | 규칙 |
|---|---|
| 룩어헤드 방지 | 모든 레짐 변수는 `t`일 종가까지로 계산하고, 실제 매매는 `t+1`부터 |
| 시총순위 사용 | `시총순위_t`로 버킷 분류해도 됨. 단, `t`일 종가 기준 레짐이면 진입은 `t+1` |
| 신규상장주 | `sma60`, `sma240`, `pct252` 계산 불가 종목은 해당 지표 계산/브레드스 집계에서 제외 |
| 브레드스 모수 | 각 비율 계산 시 분모는 해당 조건을 계산할 수 있는 종목 수 |
| 거래정지/이상치 | `거래량<=0` 또는 가격 결측 종목은 `U_t`에서 제외 |

---

## 7) 가장 실전적인 최소 버전

처음엔 아래 3개만으로 시작해도 된다.

| 분류 | 조건 |
|---|---|
| `PANIC_REBOUND_RISK` | `(RV20_pct252 >= 0.85) OR (DD60 <= -0.10 AND PctAbove20_all < 0.35)` |
| `BROAD_BULL_BREAKOUT` | `(TrendScore >= 2) AND (PctAbove60_all >= 0.55) AND (AAR5 >= 0.55)` |
| `OTHER` | 나머지 |

그리고 성과가 보이면 그다음에 `QUIET_SQUEEZE_EXPANSION`, `NARROW_LEADERSHIP`를 추가하는 게 좋다.

다음 단계는 이 표를 그대로 **판다스 컬럼 계산 코드**로 옮기는 것이다.

