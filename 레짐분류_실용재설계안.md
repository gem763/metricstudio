## 목적

이 문서는 시장을 "잘 설명"하는 분류보다, `src/pattern.py`에 있는 패턴들을 언제 켜야 실제 성과가 나는지를 기준으로 레짐을 재설계한 실전안이다.

핵심 목표는 다음 3가지다.

1. 패턴군 간에 실제로 의사결정이 달라지는 상위 레짐만 남긴다.
2. 이미 패턴이 담고 있는 정보와 중복되는 레짐 분기를 줄인다.
3. 표본 수를 지나치게 잘게 쪼개는 과최적화형 해상도를 피한다.

---

## 1) 현재 `pattern.py` 패턴군 진단

현재 구현된 진입 패턴은 대략 아래처럼 나뉜다.

### Trend / Breakout 계열

- `Bollinger(trigger='breakout_up')`
- `High(window=..., threshold=...)`
- `Trending(trigger='breakout_up')`
- `Trending(trigger='ma_trend_up')`
- `GoldenCross(...)`
- `MFI(trigger='above')`

특징:

- 전부 "상향 추세가 이미 형성되었거나, 막 확정되는 구간"에 반응한다.
- 특히 `Bollinger + 좁은폭 + 52주고가 + 이평상향 + MFI above` 같은 조합은 종목 단에서 이미
  - 압축
  - 돌파
  - 추세 정렬
  - 수급/거래강도 확인
  를 대부분 끝낸다.
- 즉 시장 레짐이 다시 `quiet vs broad`처럼 같은 축의 미세 해상도를 제공해도 추가정보가 작을 수 있다.

### Mean Reversion / Contrarian 계열

- `Disparity(threshold < 1.0)`
- `MFI(trigger='oversold_rebound')`
- `MFI(trigger='bullish_failure_swing')`
- `Bollinger(trigger='near_down')`를 이용한 하단 근접 조합

특징:

- 존재는 하지만 trend 계열보다 표현력이 얕다.
- 현재는 "과매도/이격/반등"을 잡는 최소한의 재료만 있고,
  panic 이후 반전이나 변동성 장세 전용 패턴은 아직 부족하다.

### 보조 필터 / 구조 계열

- `SizeBucket`
- `Regime.when(...)`
- `CombinedPattern (+)`
- `UnionPattern (|)`

특징:

- 자체 알파보다 "언제/어디서 켤지"를 조정하는 도구다.
- 따라서 상위 레짐 설계는 이 계층과 잘 맞물려야 한다.

---

## 2) 현재 5레짐 설계가 과한 이유

기존 설계는 다음 장점이 있다.

- 시장 상태를 서술적으로 잘 나눈다.
- `quiet`, `broad`, `narrow`, `panic`, `sideways`가 직관적이다.

하지만 실전 필터로는 아래 문제가 있다.

1. `QUIET_SQUEEZE_EXPANSION`와 `BROAD_BULL_BREAKOUT`가 둘 다 trend-friendly 축에 있다.
2. 현재 주력 패턴이 이미 종목 단에서 squeeze/breakout/trend를 강하게 요구한다.
3. 그 결과 시장 레짐도 같은 축을 다시 자르면서 표본만 줄이고, 실제 decision boundary는 크게 안 바뀔 수 있다.
4. `QUIET`처럼 평균 지속이 짧고 표본이 적은 레짐을 상위 분기로 쓰면 전략 판단이 불안정해진다.
5. `NARROW`는 시장 국면이라기보다 "대형주 편중"이라는 추가 설명에 더 가깝다.

즉 현재 5레짐은 "설명용 taxonomy"로는 유효할 수 있지만, "패턴 스위치"로는 해상도가 과하다.

---

## 3) 재설계 원칙

상위 레짐은 아래 질문에 답해야 한다.

1. 지금 시장에서 trend-following 패턴을 켜는 게 유리한가?
2. 지금 시장에서 mean-reversion 패턴을 켜는 게 유리한가?
3. 지금 시장이 panic이라서 대부분의 전략을 꺼야 하는가?
4. 위 셋 어느 쪽도 아니어서 neutral/other로 두는 게 맞는가?

즉 상위 레짐은 "시장 묘사"보다 "어떤 전략 family를 켤지"를 먼저 결정해야 한다.

---

## 4) 권장 상위 레짐 구조

### 상위 4분류

1. `TREND_FRIENDLY`
2. `CONTRARIAN_FRIENDLY`
3. `PANIC`
4. `NEUTRAL`

### 보조 태그

- `quiet_tag`
- `narrow_tag`

핵심은 `QUIET`, `BROAD`, `NARROW`를 모두 1급 레짐으로 유지하지 않고, 일부는 `TREND_FRIENDLY` 내부 태그로 내리는 것이다.

---

## 5) 왜 이런 구조가 맞는가

### `TREND_FRIENDLY`

이 레짐의 역할은 breakout / high / trend / gc / mfi-above 같은 패턴군을 켜는 것이다.

현재 주력 패턴이 여기에 속한다.

중요한 점:

- `QUIET`와 `BROAD`는 둘 다 결국 trend 패턴을 켜는 방향으로 해석된다.
- 차이는 trend-friendly 안에서도 "압축형", "확산형"의 하위 속성 차이다.
- 이 정도 해상도는 top-level regime보다 tag로 다루는 편이 더 실용적이다.

### `CONTRARIAN_FRIENDLY`

이 레짐의 역할은 disparity / oversold rebound / failure swing 같은 역추세 패턴군을 켜는 것이다.

중요한 점:

- 이건 trend와 거의 정반대 family다.
- 따라서 상위 레짐으로 분리할 가치가 충분하다.

### `PANIC`

이 레짐은 시장이 망가진 구간을 분리하는 역할이다.

중요한 점:

- 여기서는 대부분의 전략을 꺼야 한다.
- 단, 나중에 panic 전용 rebound pattern을 추가하면 예외적으로 켤 수 있다.

### `NEUTRAL`

위 셋 어느 쪽도 아닌 구간이다.

중요한 점:

- 억지로 해석하지 않는다.
- 전략은 기본적으로 off 또는 매우 제한적으로만 켠다.

---

## 6) 구체적 정의안

아래 정의는 현재 `src/regime.py`가 이미 계산하는 변수만으로 구현 가능하게 설계했다.

### 6.1 `PANIC`

기존 panic 정의를 거의 유지한다.

```python
panic = (
    (rv20_pct240 >= 0.85)
    or ((dd60 <= -0.10) and (pct_above20_all < 0.35))
)
```

### 6.2 `TREND_FRIENDLY`

기존 `BROAD`와 `QUIET`를 하나의 상위 trend 레짐으로 통합하되, 너무 이른 quiet를 막기 위해 최소 절대 강도를 추가한다.

```python
trend_friendly = (
    (trend_score >= 2)
    and (dd60 > -0.08)
    and (rv20_pct240 < 0.80)
    and (
        (
            (pct_above60_all >= 0.55)
            and (aar5 >= 0.55)
        )
        or
        (
            (bbw20_pct240 <= 0.20)
            and (rv20_pct240 <= 0.40)
            and (pct_above20_delta5 >= 0.05)
            and (pct_above20_all >= 0.45)
        )
    )
)
```

설명:

- 첫 번째 괄호는 기존 broad 성격이다.
- 두 번째 괄호는 quiet 성격이지만, `TrendScore >= 2`와 `PctAbove20_all >= 0.45`를 넣어 너무 초기/허약한 quiet를 줄인다.

### 6.3 `CONTRARIAN_FRIENDLY`

panic은 아니지만, 추세 추종보다 mean-reversion이 유리한 구간을 분리한다.

```python
contrarian_friendly = (
    (not panic)
    and (not trend_friendly)
    and (
        (
            (dd60 <= -0.05)
            and (pct_above20_all <= 0.45)
            and (pct_above20_delta5 >= -0.03)
        )
        or
        (
            (rv20_pct240 >= 0.55)
            and (pct_above20_all >= 0.30)
            and (pct_above20_all <= 0.50)
            and (aar5 >= 0.45)
        )
    )
)
```

설명:

- 핵심은 "망가졌지만 panic은 아닌", 그리고 "추세 추종보다 반등/이격 해소가 더 그럴듯한" 환경이다.
- `pct_above20_delta5 >= -0.03`는 자유낙하를 피하기 위한 최소 안정화 조건이다.

### 6.4 `NEUTRAL`

```python
neutral = (not panic) and (not trend_friendly) and (not contrarian_friendly)
```

---

## 7) 보조 태그 정의안

### `quiet_tag`

```python
quiet_tag = (
    (bbw20_pct240 <= 0.20)
    and (rv20_pct240 <= 0.40)
)
```

용도:

- trend-friendly 안에서도 squeeze형 breakout 패턴의 파라미터를 조정할 때 사용
- 예: `bandwidth_max`를 조금 완화하거나, 보유기간을 더 길게 보는 등

### `narrow_tag`

```python
narrow_tag = (
    (trend_score >= 2)
    and (large_pct_above60 >= 0.60)
    and (small_pct_above60 <= 0.45)
    and (leadership_spread >= 0.15)
)
```

용도:

- 대형주 편중 경고
- breakout 패턴이라도 `large` 위주로만 허용하거나, 중소형주엔 stricter filter 부여

중요:

- `narrow`는 top-level regime보다 size-bias tag로 해석하는 편이 맞다.

---

## 8) 패턴군별 권장 레짐 매핑

### `TREND_FRIENDLY`에서 켤 패턴

- `Bollinger(trigger='breakout_up')`
- `High(...)`
- `Trending(trigger='breakout_up')`
- `Trending(trigger='ma_trend_up')`
- `GoldenCross(...)`
- `MFI(trigger='above')`
- 위 패턴들의 조합

대표 예시:

- `볼린저돌파 + 좁은폭 + 52주고가 + 이평상향 + MFI`
- `볼린저돌파 + 52주고가`
- `GoldenCross + High`

### `CONTRARIAN_FRIENDLY`에서 켤 패턴

- `Disparity(...)`
- `MFI(trigger='oversold_rebound')`
- `MFI(trigger='bullish_failure_swing')`
- `Bollinger(trigger='near_down')` 기반 반등 조합

대표 예시:

- `Disparity(20).on(threshold=0.9)`
- `MFI(...).on(trigger='oversold_rebound')`
- `Bollinger(near_down) + MFI oversold_rebound`

### `PANIC`에서 켤 패턴

기본 정책:

- 원칙적으로 off

예외:

- 나중에 panic 전용 rebound pattern을 추가하면 매우 제한적으로 허용

### `NEUTRAL`에서 켤 패턴

기본 정책:

- 원칙적으로 off
- 필요하면 샘플 검증이 충분한 고신뢰 패턴만 허용

---

## 9) 현재 패턴 세트의 부족한 점

현재 패턴 세트는 trend 쪽은 꽤 강하지만, contrarian/panic 쪽은 아직 얇다.

### 부족 1: 상대강도(Relative Strength) 계열 부재

현재 breakout 계열은 절대 가격과 밴드/고가 중심이다.

부족한 이유:

- 시장이 trend-friendly여도, 모든 breakout이 같은 질은 아니다.
- 진짜 leader를 구분하려면 종목이 시장보다 강한지 보는 지표가 필요하다.

추천 추가 패턴:

- `RelativeStrength`

예시 개념:

```python
RelativeStrength(name='상대강도상위').on(
    market='kospi',
    window=60,
    threshold=0.0,
)
```

의미:

- 최근 60일 종목 수익률이 시장 수익률보다 높은 종목만 선별
- trend regime에서 breakout의 질을 올리는 데 매우 유용

### 부족 2: 거래대금/거래량 돌파 확인 패턴 부재

현재 volume 정보는 `MFI`에 간접적으로만 들어간다.

부족한 이유:

- breakout 성공 여부는 종종 가격보다 거래 폭발에서 갈린다.
- 특히 quiet/broad 내부에서 "진짜 돌파"와 "조용한 미끄러짐"을 구분할 도구가 필요하다.

추천 추가 패턴:

- `VolumeSpike` 또는 `AmountSurge`

예시 개념:

```python
AmountSurge(name='거래대금급증').on(
    window=20,
    threshold=2.0,
)
```

의미:

- 최근 20일 평균 대비 거래대금이 2배 이상인 날
- breakout 계열과 조합 시 실전성이 높다

### 부족 3: 추세 continuation / retest 패턴 부재

현재 trend 계열은 "초기 돌파" 쪽이 강하고, "돌파 후 눌림 재진입" 패턴은 약하다.

부족한 이유:

- trend regime에서 가장 안정적인 수익은 종종 초기 돌파보다 눌림 continuation에서 나온다.

추천 추가 패턴:

- `Pullback`
- `RetestBreakout`

예시 개념:

```python
RetestBreakout(name='돌파후눌림').on(
    breakout_window=20,
    retest_tolerance=0.03,
    rebound_confirm='close_up',
)
```

의미:

- 고점 돌파 후 밴드/이평/이전고점 부근 눌림에서 다시 올라서는 패턴
- trend regime의 second entry 도구로 유용

### 부족 4: panic 전용 반등 패턴 부재

현재 contrarian 패턴은 panic 구간까지 커버하기엔 약하다.

부족한 이유:

- panic은 ordinary contrarian과 다른 환경이다.
- 단순 이격도/MFI만으로는 칼날잡기가 되기 쉽다.

추천 추가 패턴:

- `PanicRebound`

예시 개념:

```python
PanicRebound(name='패닉반등').on(
    drawdown_window=20,
    drawdown_min=-0.18,
    rebound_days=3,
    volume_spike=True,
)
```

의미:

- 짧은 기간 급락 후, 거래량 급증을 동반한 반등만 잡는 전용 패턴
- `PANIC` 레짐의 유일한 예외전략으로 쓰기 좋다

---

## 10) 추가 패턴 우선순위

실전 우선순위는 아래 순서를 권장한다.

1. `RelativeStrength`
2. `AmountSurge` 또는 `VolumeSpike`
3. `RetestBreakout`
4. `PanicRebound`

이유:

- 지금 주력 전략군이 trend 쪽이므로, 먼저 trend family의 quality filter를 보강하는 게 ROI가 가장 높다.
- contrarian/panic 확장은 그 다음 단계가 맞다.

---

## 10.5) 1차 검증 메모

`trend_friendly` 레짐 안에서 현재 주력 패턴

- `Bollinger breakout(0.05)`
- `52주 고가`
- `MA200 상향`
- `MFI > 50`

조합을 기준으로 간단 검증한 결과는 아래와 같다.

### `RelativeStrength`

- 구현: 최근 `window`일 종목 수익률 - 시장 수익률
- 테스트:
  - `RS60 >= 0`
  - `RS60 >= 5%p`
  - `RS120 >= 0`

관찰:

- 표본 수는 `75% ~ 87%` 수준으로 줄었다.
- 그러나 중기(2M~6M) 기준으로는 기본 패턴을 일관되게 이기지 못했다.
- 즉 현재 breakout 주력 조합에서는 `RelativeStrength`가 1차 필수 필터라기보다, 후순위 보조 필터에 가깝다.

### `AmountSurge`

- 구현: 당일 거래대금 / 최근 20일 평균 거래대금
- 테스트:
  - `AmountSurge(window=20, threshold=1.5)`
  - `AmountSurge(window=20, threshold=2.0)`

관찰:

- `1.5x`는 표본을 약 `87%` 수준으로만 줄이면서,
  `1M ~ 6M` 전 구간에서 기본 패턴보다 더 높은 기하 연율화 초과성과를 보였다.
- `2.0x`는 오히려 너무 강해서, 표본 감소 대비 개선 폭이 작았다.
- 따라서 현재 실전 우선 조합은 `RelativeStrength`보다 `AmountSurge(20, 1.5x)`가 더 적합하다.

### 결론

현재 trend family의 보강 우선순위는 아래처럼 수정하는 편이 맞다.

1. `AmountSurge(20, 1.5x)`
2. `RelativeStrength`
3. `RetestBreakout`
4. `PanicRebound`

즉 `RelativeStrength`는 아이디어 자체는 맞지만,
지금 주력 breakout 조합에서는 거래 확인 필터보다 앞에 둘 정도로 강하지 않았다.

---

## 10.6) Walk-Forward 점검

`trend_friendly` 레짐 안에서 아래 두 조합을 비교했다.

- `기본패턴`
- `기본패턴 + AmountSurge(20, 1.5x)`

비교 지표는 `after cost geometric annualized gap vs regime benchmark`다.

### 구간별 관찰

#### 2000-2006

- `Amount1.5x`가 `1M ~ 6M` 전 구간에서 기본패턴보다 우세했다.
- 특히 `1M`, `2M` 개선 폭이 컸다.

#### 2007-2012

- `1M ~ 3M`은 `Amount1.5x`가 우세했다.
- `6M`은 기본패턴이 아주 근소하게 더 좋았다.
- 즉 완승은 아니지만, 중기 구간에서는 여전히 거래 확인 필터가 유효했다.

#### 2013-2018

- 전 구간에서 `Amount1.5x`가 소폭 우세했다.
- 다만 이 시기에는 개선 폭이 크지 않았다.
- 해석상 이 구간은 거래 필터의 효과가 있는 것은 맞지만, 압도적이라고 보긴 어렵다.

#### 2019-2025

- 전 구간에서 `Amount1.5x`가 우세했다.
- 특히 `1M ~ 3M`에서 개선이 다시 분명하게 나타났다.

### 실전 해석

- `AmountSurge(20, 1.5x)`는 특정 한 시기에만 먹힌 필터로 보이지 않는다.
- 다만 효과 크기는 시기마다 다르고,
  `초기 ~ 중기 horizon(1M ~ 3M)`에서 가장 일관되게 좋다.
- 따라서 현재 주력 breakout 전략에는 `AmountSurge(20, 1.5x)`를 기본 옵션으로 두는 것이 타당하다.
- 대신 `6M`까지 길게 끌고 가는 전략에서는, 거래 필터가 항상 큰 우위를 주는 것은 아니므로
  보유기간 실험과 함께 보는 것이 좋다.

### 재현 방법

반복 검증용 스크립트:

- [scripts/validate_trend_filters.py](/mnt/c/Users/USER/Documents/GitHub/metricstudio/scripts/validate_trend_filters.py)

최소 단위 동작 테스트:

- [tests/test_pattern_filters.py](/mnt/c/Users/USER/Documents/GitHub/metricstudio/tests/test_pattern_filters.py)

---

## 11) 구현 권장 방식

### Phase 1

- 상위 레짐을 `trend / contrarian / panic / neutral`로 단순화
- `quiet_tag`, `narrow_tag`는 보조 태그로만 유지

### Phase 2

- representative pattern set을 만든다.

trend 대표:

- `Bollinger breakout`
- `High`
- `GoldenCross`

contrarian 대표:

- `Disparity`
- `MFI oversold_rebound`

panic 대표:

- 일단 없음 또는 향후 `PanicRebound`

### Phase 3

- 각 레짐에서 pattern family ranking이 실제로 바뀌는지 검증

검증 기준:

1. 레짐별 top pattern family가 명확히 달라지는가
2. 레짐별 표본 수가 충분한가
3. walk-forward에서도 같은 방향이 유지되는가

---

## 12) 최종 제안

지금 당장 가장 실용적인 방향은 아래다.

1. 기존 `BROAD`와 `QUIET`를 상위 `TREND_FRIENDLY`로 통합한다.
2. `NARROW`는 top-level regime이 아니라 `narrow_tag`로 내린다.
3. `PANIC`는 유지한다.
4. `CONTRARIAN_FRIENDLY`를 새로 만든다.
5. 남는 구간은 `NEUTRAL`로 둔다.
6. `QUIET`는 삭제하지 말고 `quiet_tag`로 남긴다.
7. pattern 보강은 `RelativeStrength`, `AmountSurge`, `RetestBreakout`, `PanicRebound` 순으로 진행한다.

이 구조가 현재 `pattern.py`의 실제 성격과 가장 잘 맞고, 과한 설명용 분기를 줄이면서도 실전적인 패턴 스위치를 만들 가능성이 높다.
